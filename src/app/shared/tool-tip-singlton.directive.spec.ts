import { ToolTipSingltonDirective } from './tool-tip-singlton.directive';

describe('ToolTipSingltonDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolTipSingltonDirective();
    expect(directive).toBeTruthy();
  });
});
